<span>
    @if ($countUnreadNotifications)
    <span class="absolute rounded-full -top-1 -right-1 px-1 bg-red-700 text-white text-xs">
        {{ $countUnreadNotifications }}
    </span>
    @endif
</span>